<UserControl x:Class="Lukoil.Client.Views.ComboQueryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Opacity="0">
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.25" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Комбо-запрос" Style="{StaticResource HeaderTextStyle}" />
            <Button Grid.Column="1" Content="На главную" Command="{Binding GoHomeCommand}" />
        </Grid>

        <StackPanel Grid.Row="1" Margin="0,12,0,10">
            <TextBlock Text="{Binding ScenarioTitle}" FontWeight="SemiBold" />
            <TextBlock Text="{Binding ScenarioDescription}" Margin="0,4,0,8" Style="{StaticResource CaptionTextStyle}" TextWrapping="Wrap" />
            <Button Content="Запустить комбо-проверку" Command="{Binding RunComboCommand}" Width="260" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#FFEF5350" Margin="0,8,0,0" />
        </StackPanel>

        <Grid Grid.Row="2">
            <TabControl ItemsSource="{Binding Sections}">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Title}" />
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <Grid Margin="0,8,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="{Binding RowCount, StringFormat='Записей: {0}'}" Margin="0,0,0,8" Style="{StaticResource CaptionTextStyle}" />
                            <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="True" IsReadOnly="True" Style="{StaticResource ModernDataGridStyle}" />
                            <TextBlock Grid.Row="1"
                                       Text="{Binding Error}"
                                       Foreground="#FFEF5350"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>
