<UserControl x:Class="Lukoil.Client.Views.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="3*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBox Width="700"
                     Text="{Binding ManualCommand, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:HintAssist.Hint="Ручная команда, например GET_SALES|0" />
            <Button Content="Отправить (Enter/Ctrl+Enter)" Command="{Binding SendCommandCommand}" Margin="10,0,0,0" />
        </StackPanel>

        <GroupBox Grid.Row="1" Header="Сырой ответ сервера" Margin="0,10,0,8">
            <TextBox Text="{Binding RawResponse}" FontFamily="Consolas" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Логи">
            <DataGrid ItemsSource="{Binding Logs}" AutoGenerateColumns="True" IsReadOnly="True" />
        </GroupBox>
    </Grid>
</UserControl>
